#include <stdio.h>

#define MAX   100000
int idx;
int stack[MAX];

char result[MAX];

int main(int argc, char *argv[])
{
  int n;

  scanf("%d", &n);

  while (n--) {
    scanf("%d", &v);
    if (top() < v) {
      
  }

  return 0;
}
